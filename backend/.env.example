# Email Configuration for Password Reset
# Add these to your existing .env file

# SMTP Configuration for Gmail (or your email provider)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=deepakkgp0987@gmail.com
SMTP_PASS=dnze mpfw fozi ncsg

# From email address (usually same as SMTP_USER)
FROM_EMAIL=deepakkgp0987@gmail.com

# Note: If using Gmail, you'll need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" instead of using your regular password
# 3. Use the app password as SMTP_PASS

# For other email providers, adjust SMTP_HOST and SMTP_PORT accordingly:
# - Outlook: smtp-mail.outlook.com:587
# - Yahoo: smtp.mail.yahoo.com:587
# - Custom SMTP: your-smtp-server.com:587
